/// <reference types="react" />
import { MDCLinearProgressFoundation } from '@material/linear-progress';
import { FoundationComponent } from '@rmwc/base';
/** A component to display linear progress. */
export interface LinearProgressProps {
    /** Progress float percentage between 0 and 1. */
    progress?: number;
    /** A Progress buffer float percentage between 0 and 1. */
    buffer?: number;
    /** Progress goes from right to left. */
    reversed?: boolean;
    /** Hides the progress bar. Adding / removing this prop will trigger an animation in or out.  */
    closed?: boolean;
}
export interface DeprecatedLinearProgressProps {
    /** Whether or not the Progress bar is determinate. */
    determinate?: boolean;
}
/** A component to display linear progress. */
export declare class LinearProgress extends FoundationComponent<MDCLinearProgressFoundation, LinearProgressProps & DeprecatedLinearProgressProps> {
    static displayName: string;
    static defaultProps: {
        progress: undefined;
        buffer: undefined;
        reversed: boolean;
    };
    private root;
    determinate: boolean | null;
    getDefaultFoundation(): MDCLinearProgressFoundation;
    sync(props: LinearProgressProps, prevProps: LinearProgressProps): void;
    render(): JSX.Element;
}
